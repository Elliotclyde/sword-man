# Sword man

A completely vibe coded phaser js game using free sprites from itch.io.

Vibe coded with opencode. Originally using github copilot free plan, then very slowly using the free Nvidia API.

Run it with:

```bash
npx http-server
```

Then visit `http://localhost:8080` in your browser.
